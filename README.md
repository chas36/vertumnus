# Vertumnus

Десктопный конвертер видео в `MP4` для старых проекторов и других устройств с ограниченной поддержкой форматов.

Приложение написано на `Python + PySide6`, использует `FFmpeg/ffprobe`, поддерживает очередь файлов, выбор качества, аудиодорожек и субтитров, а также базовый и расширенный режим интерфейса.

## Что умеет

- Конвертировать видео разных форматов в `MP4 (H.264 + AAC)`
- Работать с очередью файлов и пакетной конвертацией
- Поддерживать drag-and-drop и добавление через диалог
- Сохранять настройки и очередь между запусками
- Переключаться между базовым и расширенным интерфейсом
- Выбирать профиль `Для проектора` или `Кастомный`
- Выбирать качество видео: `Оригинал / 1080p / 720p / 480p`
- Оставлять одну нужную аудиодорожку
- Оставлять одну дорожку субтитров или отключать субтитры
- Показывать историю конвертаций и детали ошибок
- Переключаться между тёмной и светлой темой
- Запускать `ffmpeg` и `ffprobe` на Windows без всплывающего консольного окна
- Показывать экран ожидания со слайдшоу из пользовательских фотографий во время конвертации

## Для чего проект

Основной сценарий: быстро подготовить видео для старых проекторов, которые плохо работают с современными контейнерами, кодеками, несколькими аудиодорожками и переключаемыми субтитрами.

Профиль `Для проектора` делает совместимый выходной файл без необходимости вручную настраивать FFmpeg.

## Быстрый старт

### macOS

Нужны:

- `Python`
- `ffmpeg`
- `ffprobe`

Установка `ffmpeg`:

```bash
brew install ffmpeg
```

Запуск приложения:

```bash
git clone https://github.com/chas36/vertumnus.git
cd vertumnus
VENV_PATH=.venv ./run_mac.sh
```

Если у тебя используется окружение `.venv311`, можно запускать и без переопределения:

```bash
./run_mac.sh
```

Сборка `.app` для macOS:

```bash
./build_mac.sh
```

Результат:

```text
dist/MP4Converter.app
```

### Windows

Сборка описана в [WINDOWS_BUILD.md](WINDOWS_BUILD.md).

Коротко:

1. Установить `Python 3.11`
2. Положить `ffmpeg.exe` и `ffprobe.exe` в `assets/ffmpeg/`
3. Выполнить `build_windows.ps1`

Для GitHub Actions это не требуется: workflow сам скачивает FFmpeg во время CI-сборки.

Если нужен уже собранный файл:

1. Открой `Actions` в GitHub
2. Запусти workflow `Build Windows`
3. Скачай artifact `MP4Converter-windows`
4. Внутри архива будет `MP4Converter.exe`

## Зависимости

Python-пакеты:

```bash
pip install -r requirements.txt
```

Системные зависимости:

- `ffmpeg`
- `ffprobe`

Для локального запуска они должны быть доступны в `PATH` или лежать в `assets/ffmpeg`.

## Структура проекта

```text
.
├── core/        # FFmpeg, ffprobe, воркер, история, состояние очереди
├── models/      # Dataclass-модели настроек, файлов и потоков
├── tests/       # Тесты конвертера, probe и состояния очереди
├── ui/          # Главное окно, список файлов, панель настроек, темы, экран ожидания
├── main.py      # Точка входа
├── build_mac.sh # Сборка .app для macOS
├── run_mac.sh   # Launcher для macOS
└── build.spec   # Сборка через PyInstaller
```

## Текущие возможности интерфейса

- Базовый режим:
  - добавить файлы
  - выбрать профиль
  - запустить конвертацию
- Расширенный режим:
  - выбрать папку вывода
  - настроить качество, FPS и битрейты
  - выбрать аудиодорожку
  - выбрать субтитры
  - управлять темой интерфейса
  - выбрать папку с фотографиями для экрана ожидания

## Экран ожидания с фотографиями

Во время конвертации приложение может показывать слайдшоу из твоих фотографий.

Как использовать:

1. Скопируй фотографии с iPhone в обычную папку на компьютере
2. Лучше использовать форматы `JPG`, `PNG`, `WEBP` или `BMP`
3. В приложении включи `Расширенный режим`
4. В блоке `Экран ожидания` выбери папку с фотографиями
5. Запусти конвертацию

Если фото на iPhone в формате `HEIC`, проще всего сначала экспортировать их в `JPEG`.

## Совместимость профиля "Для проектора"

Профиль рассчитан на старые устройства и даёт максимально безопасный выходной файл:

- контейнер `MP4`
- видео `H.264`
- звук `AAC`
- одна выбранная аудиодорожка
- опционально одна дорожка текстовых субтитров

Именно поэтому для файлов с несколькими озвучками и субтитрами пользователь явно выбирает, что оставить в результате.

## Ограничения

- Сборка `Windows .exe` должна выполняться на Windows
- Image-based субтитры вроде `PGS` не встраиваются в `MP4`
- `run_mac.sh` по умолчанию смотрит в `.venv311`, если не передать `VENV_PATH`
- Лицензия проекта пока не выбрана и не добавлена в репозиторий

## Разработка

Запуск тестов:

```bash
.venv/bin/python -m unittest discover -s tests -v
```

Проверка компиляции:

```bash
.venv/bin/python -m compileall main.py core models ui tests
```

## Вклад в проект

Правила и минимальный workflow описаны в [CONTRIBUTING.md](CONTRIBUTING.md).

## Roadmap

- Улучшить экран пустого состояния
- Добавить более явный общий прогресс очереди
- Добавить иконки и скриншоты в README
